# Exercises for this module

Each exercise is its own crate.
The `src/main.rs` file is where you should start.
If you need help, look at `src/answer.rs`

## Running the code

You're able to use `cargo`:

- `cargo run` runs the code in `src/main.rs`
- `cargo run --bin answer` runs the code in `src/answer.rs`

